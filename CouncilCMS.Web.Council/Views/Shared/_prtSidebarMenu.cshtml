@model CmsMenu

@if (Model != null)
{
    <div class="cms-sidebar-menu">
        <div class="title">@Model.Name</div>
        <ul>
            @foreach (var item in Model.Items)
            {
                @Html.Partial("_prtSimpleMenuItem", item)
            }
        </ul>
    </div>
}


